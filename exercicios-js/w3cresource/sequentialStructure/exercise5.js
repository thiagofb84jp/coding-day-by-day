/**
 * Faça um Programa que converta metros para centímetros.
 */

function convertMetersToCent(meter) {
    let centimeters = meter * 100;

    console.log(meter + 'm equivale a ' + centimeters + 'cm');
}

convertMetersToCent(8.54);